import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'art.acma.portolanopo',
  appName: 'Portolano del Po',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
